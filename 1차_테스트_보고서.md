# EKF_basic 1차 테스트 보고서

**일시**: 2026-04-26  
**모델**: `out_classifier2/checkpoints/best.pth` (window_len=100, 이전 버전 학습)  
**테스트 환경**: CPU, Oxford 24컬럼 포맷, 100Hz

---

## 1. 모델 학습 요약 (train.log 기반)

| 지표 | 훈련 세트 | 검증 세트 | 테스트 세트 |
|---|---|---|---|
| Regression RMSE | 0.1799 m | 0.1769 m | 0.1712 m |
| Classification Acc | — | 0.924 (92.4%) | 0.939 (93.9%) |
| Classification Loss | 0.3269 | 0.6144 | 0.5457 |

- 분류 정확도 93.9%: 학습 자체는 성공적
- regression RMSE 0.1712 m (1초 윈도우 기준): 양호한 수준
- cls loss 최종 수렴 0.3 대 → 모델이 분류 학습을 병행 완료

---

## 2. 시각화 테스트 결과

### 테스트 명령

```bash
# 테스트 1 - oxford_large_scale_6 (기본 파라미터)
python View/visualize_comparison.py \
  --data_path TLIO_Oxford_Dataset/oxford_large_scale_6/imu0_resampled.npy \
  --model_path outputs/out_classifier2/checkpoints/best.pth \
  --norm_mean outputs/out_classifier2/norm_mean.npy \
  --norm_std outputs/out_classifier2/norm_std.npy \
  --window_len 100

# 테스트 2 - oxford_handheld_21 (README 권장 파라미터)
python View/visualize_comparison.py ... \
  --meascov_scale 0.1 --init_vel_sigma 0.01 --sigma_na 0.2 --sigma_ng 0.01

# 테스트 3 - oxford_handbag_1 (기본 파라미터)
python View/visualize_comparison.py ...
```

### RMSE 결과

| 시퀀스 | 클래스 | Network RMSE_XY | EKF RMSE_XY | EKF/Net 비율 |
|---|---|---|---|---|
| oxford_large_scale_6 | 미지 클래스 | **1.060 m** | 6.972 m | 6.6× 악화 |
| oxford_handheld_21 | handheld | **1.232 m** | 6.350 m | 5.2× 악화 |
| oxford_handbag_1 | handbag | 3.022 m | **2.553 m** | 0.84× 개선 |

**handbag_1만 EKF가 Network보다 나음.** 나머지 2건은 EKF가 Network보다 5~7배 나쁨.

---

## 3. 공통 경고 패턴

```
WARNING - Deactivating Mahalanobis test, because failed for too long.
```

모든 시퀀스에서 수백 회 반복 발생. 필터 수렴 직후부터 Mahalanobis 게이팅이 지속 실패함. 이는 측정값이 필터 예측과 체계적으로 불일치함을 의미한다.

---

## 4. 원인 분석

### 4-1. meascov_scale 값이 과소 설정됨 (주요 원인)

현재 STATE_EKF_PARAMS:

| 클래스 | meascov_scale |
|---|---|
| unknown (-1) | 0.001 |
| handbag (1) | 0.05 |
| handheld (2) | 0.01 |
| pocket (3) | 0.005 |
| running (4) | 0.05 |
| slow-walking (5) | 0.005 |
| trolley (6) | 0.02 |

네트워크 출력의 log_var는 약 −4 ~ −2 수준 → exp(log_var) ≈ 0.018 ~ 0.135 m².  
예: handheld(0.01) × exp(−4) = 0.00018 m² → 측정 표준편차 **0.013 m**  
실제 네트워크 RMSE는 0.17 m 수준이므로 측정 공분산이 **10배 이상 과소** 설정됨.  
필터가 네트워크 측정을 과신 → Mahalanobis 게이팅 연속 실패 → 발산.

### 4-2. `--meascov_scale` CLI 인자 무시 버그

`run_ekf_imutracker()` 내부에서:
```python
ekf_cfg = SimpleNamespace(
    ...
    meascov_scale = 1.0,   # ← 하드코딩. args.meascov_scale 미반영
)
```
`scekf.py`는 `ekf_cfg.meascov_scale`을 추가 배율로 곱하므로,  
실질적 측정 공분산 = STATE_EKF_PARAMS.meascov_scale × exp(log_var) × ekf_cfg.meascov_scale  
= STATE_EKF_PARAMS.meascov_scale × exp(log_var) × **1.0** (CLI 인자 무효)

README의 `--meascov_scale 0.1` 옵션이 전혀 작동하지 않음.

### 4-3. large_scale 미지 클래스 처리

`oxford_large_scale_*` 시퀀스는 훈련 데이터에 없는 클래스.  
분류기가 임의 클래스를 예측하거나 noise(7)로 예측 시 `state_id=-1`(unknown)로 처리돼  
`meascov_scale=0.001`이 적용 → 필터가 측정을 극도로 과신 → 발산 가속.

### 4-4. noise 클래스(7)가 STATE_EKF_PARAMS에 없음

LABEL_REMAP에서 noise는 class index 6 (0-based) = pred_class 7 (1-based).  
STATE_EKF_PARAMS에 key 7이 없어 noise 예측 시 항상 state_id=-1로 fallback.

### 4-5. 현재 모델은 이전 버그 상태에서 학습됨

`out_classifier2/best.pth`는 이번 세션에서 수정한 버그들(키 불일치, window_len 불일치)이 적용되기 **전**에 학습된 모델:
- 분류기 사전학습 이식 실패 (pose_classifier 랜덤 초기화)
- extractor 첫 번째 Linear 랜덤 초기화 (window_len 불일치로 shape 미스매치)
- 그럼에도 100 epoch 학습으로 acc=93.9% 달성

---

## 5. 수정 필요 사항

### 5-1. 즉시 수정: meascov_scale 값 조정

`visualize_comparison.py` STATE_EKF_PARAMS 권장 변경:

| 클래스 | 현재 | 권장 | 이유 |
|---|---|---|---|
| unknown (-1) | 0.001 | **0.5** | 과소신뢰 → 보수적으로 |
| handbag (1) | 0.05 | **1.0** | 흔들림 큰 클래스 |
| handheld (2) | 0.01 | **0.5** | 현재 발산 확인 |
| pocket (3) | 0.005 | **0.3** | 안정적이나 여전히 과소 |
| running (4) | 0.05 | **2.0** | 충격 큰 클래스 |
| slow-walking (5) | 0.005 | **0.3** | 안정적 |
| trolley (6) | 0.02 | **0.8** | 진동 클래스 |

### 5-2. 즉시 수정: `run_ekf_imutracker`의 meascov_scale 버그

```python
ekf_cfg = SimpleNamespace(
    ...
    meascov_scale = meascov_scale,   # args.meascov_scale 반영
    ...
)
```

### 5-3. STATE_EKF_PARAMS에 noise 클래스 추가

```python
7: dict(meascov_scale=5.0, sigma_na=None, sigma_ng=None, ita_ba=None, ita_bg=None),  # noise
```

### 5-4. 재학습 권장

이번 세션에서 수정한 사항들(classifier key, window_len=200, early stopping, patch_len)을 반영한 재학습 필요:
```bash
cd src
python Network/train.py --output_dir outputs/out_classifier3
```

---

## 6. 테스트 환경 파라미터 기록

| 파라미터 | 값 |
|---|---|
| window_len | 100 (현재 모델 기준) |
| update_freq | 1.0 Hz |
| sigma_na | sqrt(1e-3) ≈ 0.03162 |
| sigma_ng | sqrt(1e-4) ≈ 0.01 |
| ita_ba | 1e-4 |
| ita_bg | 1e-6 |
| init_vel_sigma | 1.0 (테스트1,3) / 0.01 (테스트2) |
| meascov_scale (ekf_cfg) | 1.0 (하드코딩, CLI 무시됨) |
| meascov_scale (state) | STATE_EKF_PARAMS 기준 |

---

## 7. 다음 단계

1. **meascov_scale 조정** → EKF 재테스트 (이번 세션 즉시 가능)
2. **meascov_scale CLI 버그 수정** → run_ekf_imutracker 수정
3. **모델 재학습** → window_len=200, 수정된 사전학습 이식으로 out_classifier3 생성
4. **재학습 후 테스트** → window_len=200 기준으로 동일 시퀀스 재평가
