<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_imu_test"
        android:title="IMU 센서 진단"
        app:showAsAction="never"/>
    <item
        android:id="@+id/action_export"
        android:title="경로 내보내기"
        app:showAsAction="never"/>
    <!-- [P87] GT 마크 내보내기 -->
    <item
        android:id="@+id/action_export_marks"
        android:title="마크 내보내기 (GT)"
        app:showAsAction="never"/>
    <!-- [P85] 단위보정(A) 런타임 토글 — replay A/B 비교용 -->
    <item
        android:id="@+id/action_toggle_unitfix"
        android:title="단위보정(A): ON"
        app:showAsAction="never"/>
    <!-- [P84] 평면도 오버레이 (approach A — 측정/GT용) -->
    <item
        android:id="@+id/action_toggle_floorplan"
        android:title="표시 모드: 격자 → 평면도"
        app:showAsAction="never"/>
    <item
        android:id="@+id/action_load_floorplan"
        android:title="평면도 불러오기"
        app:showAsAction="never"/>
    <!-- [P88e] 보정 절차 → 직접 정렬(드래그·핀치·회전)로 대체 -->
    <item
        android:id="@+id/action_calib_floorplan"
        android:title="평면도 정렬 (드래그·핀치·회전)"
        app:showAsAction="never"/>
    <item
        android:id="@+id/action_toggle_checkpoint"
        android:title="체크포인트 기록 시작"
        app:showAsAction="never"/>
    <item
        android:id="@+id/action_export_checkpoint"
        android:title="체크포인트 내보내기"
        app:showAsAction="never"/>
    <!-- [P88f] 지도 관련 메뉴 비활성화 (작품사진 완료, MAP_ENABLED=false 와 동기).
         재활성화: 두 항목 visible="true" + MainActivity.MAP_ENABLED=true. -->
    <item
        android:id="@+id/action_toggle_view"
        android:title="표시 모드: 격자 → 지도"
        android:visible="false"
        app:showAsAction="never"/>
    <item
        android:id="@+id/action_rotate_map"
        android:title="지도 경로 회전 +90°"
        android:visible="false"
        app:showAsAction="never"/>
    <item
        android:id="@+id/action_toggle_ekf"
        android:title="전처리 OFF 궤적 표시"
        android:visible="false"
        app:showAsAction="never"/>
</menu>
