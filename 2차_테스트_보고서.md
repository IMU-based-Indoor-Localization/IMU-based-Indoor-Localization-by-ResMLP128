# EKF_basic 2차 테스트 보고서

**일시**: 2026-04-26  
**모델**: `out_classifier2/checkpoints/best.pth` (window_len=100, 1차와 동일)  
**테스트 환경**: CPU, Oxford 24컬럼 포맷, 100Hz  
**목적**: 1차 버그 수정 후 재테스트 — meascov_scale CLI 버그, noise 클래스 누락, STATE_EKF_PARAMS 값 조정

---

## 1. 1차 대비 변경 사항

| 항목 | 1차 상태 | 2차 수정 |
|---|---|---|
| `run_ekf_imutracker` meascov_scale | 하드코딩 1.0 (CLI 무시) | `ekf_cfg.meascov_scale = args.meascov_scale` |
| noise 클래스(7) | STATE_EKF_PARAMS 에 없음 → state_id=-1 fallback | `7: dict(meascov_scale=5.0, ...)` 추가 |
| CLI `--meascov_scale` 기본값 | 10.0 (버그 상태) | 1.0 으로 수정 |
| `mahalanobis_fail_scale` | 0 | 0 (유지) |
| STATE_EKF_PARAMS 값 | 0.001~0.05 (매우 작음) | 0.3~5.0 (1차 보고서 권장값으로 상향) |

**최종 STATE_EKF_PARAMS (2차 적용)**:

| 클래스 | 1차 STATE_scale | 2차 STATE_scale | 비율 |
|---|---|---|---|
| unknown (-1) | 0.001 | 0.5 | ×500 |
| handbag (1) | 0.05 | 1.0 | ×20 |
| handheld (2) | 0.01 | 0.5 | ×50 |
| pocket (3) | 0.005 | 0.3 | ×60 |
| running (4) | 0.05 | 2.0 | ×40 |
| slow-walk (5) | 0.005 | 0.3 | ×60 |
| trolley (6) | 0.02 | 0.8 | ×40 |
| noise (7) | (없음, 0.001 fallback) | 5.0 | 신규 |

---

## 2. 시각화 테스트 결과

```bash
# 공통 파라미터 (meascov_scale CLI 기본값 = 1.0)
python View/visualize_comparison.py \
  --data_path TLIO_Oxford_Dataset/<seq>/imu0_resampled.npy \
  --model_path outputs/out_classifier2/checkpoints/best.pth \
  --norm_mean outputs/out_classifier2/norm_mean.npy \
  --norm_std outputs/out_classifier2/norm_std.npy \
  --window_len 100 \
  --save ../result/test2_<seq>.png
```

### RMSE 결과 비교

| 시퀀스 | Network RMSE_XY | EKF 1차 | EKF 2차 | 변화 |
|---|---|---|---|---|
| oxford_large_scale_6 | **1.060 m** | 6.972 m | 19.196 m | ↑ 2.75× 악화 |
| oxford_handheld_21   | **1.232 m** | 6.350 m | 13.056 m | ↑ 2.06× 악화 |
| oxford_handbag_1     | 3.022 m     | **2.553 m** | 28.473 m | ↑ 11.1× 악화 |

**2차에서 전 시퀀스 결과 악화.** handbag_1은 1차 대비 11배 나빠짐.

시각화 파일: `result/test2_large_scale_6.png`, `result/test2_handheld_21.png`, `result/test2_handbag_1.png`

---

## 3. 원인 분석

### 3-1. Mahalanobis 게이팅 사이클과 1차 handbag 성공의 실제 메커니즘

`scekf.py` 게이팅 로직(line 360~378) 분석:

```
수렴(10초) 후 게이팅 활성화 →
게이팅 실패 → 0.5초 경과 → "Deactivating" 로그 출력
  → force_mahalanobis_until = 현재시각 + 1초 (비활성화 기간)
  → 비활성화 기간: 테스트 없이 업데이트 적용
  → 다시 활성화 → 실패 → ... (반복)
```

**1차 handbag 성공(2.553m)의 실제 이유:**

1차에서 STATE_scale=0.05 (매우 작음) → R_diag ≈ 0.05 × 0.135 = 0.00675 m² (handbag, exp(log_var≈-2))  
→ d² = (0.17m)² / (0.00675) × 3 ≈ 12.8 → Mahalanobis 임계값(11.345) 초과 → **게이팅 실패**

하지만 0.5초 후 비활성화 구간(1초) 동안 게이팅 없이 업데이트 적용:  
R이 매우 작으므로 Kalman 이득 K ≈ 1 → **필터가 네트워크 측정값을 거의 완전히 신뢰**  
→ EKF가 사실상 네트워크 dead-reckoning 모드로 동작  
→ handbag 에 대해 좋은 네트워크 예측 → 좋은 결과

즉, **1차 handbag 성공은 우연적인 구성 덕분**이었다: 과소 R이 Mahalanobis를 실패시키고, 실패→비활성화→재적용 사이클이 네트워크 dead-reckoning을 근사.

### 3-2. 2차에서 악화된 이유

2차에서 STATE_scale 값을 0.5~1.0으로 상향:

| 구간 | STATE_scale=0.05 (1차) | STATE_scale=1.0 (2차) |
|---|---|---|
| R_diag (handbag, log_var=-2) | 0.00675 m² | 0.135 m² |
| Mahalanobis d² | ≈ 12.8 (초과) | ≈ 0.64 (통과) |
| 결과 | 비활성화 사이클 → K≈1 → 네트워크 추종 | 정상 통과 → 작은 K → 미세 보정 |

→ Mahalanobis가 "정상적으로" 통과하게 되어, 작은 Kalman 이득으로 미세 보정만 적용  
→ IMU 적분 오차가 누적, 제대로 보정되지 않음 → **28.473m 폭발**

### 3-3. handheld / large_scale 개선 불가 원인

large_scale_6: 훈련 데이터 없는 OOD 클래스 → 분류기가 unknown(-1)으로 처리  
handheld_21: 분류 정확하더라도 개별 윈도우 예측이 체계적으로 편향(systematic bias)

**EKF는 랜덤 오차를 평균화할 수 있지만, 체계적 편향은 오히려 필터에 누적시킴.**  
현재 모델 수준에서는 파라미터 조정만으로 EKF 개선이 불가능.

### 3-4. 핵심 발견: EKF 파라미터가 아닌 모델 품질이 병목

```
최종 R = ekf_cfg.meascov_scale × STATE_scale × exp(log_var)
```

- R이 작으면: K≈1 → 필터가 네트워크 측정값을 그대로 사용 = 네트워크 dead-reckoning
- R이 크면: K≈0 → 필터가 IMU 적분에 의존 = IMU dead-reckoning  
- 어느 쪽도 모델 예측이 편향되어 있으면 도움이 되지 않음

---

## 4. 향후 과제

### 4-1. 단기 (파라미터 조정)

현재 시스템에서 최적 결과를 얻으려면 `mahalanobis_fail_scale=0`을 유지하되,  
각 시퀀스별 최적 STATE_scale 탐색이 필요 (handbag: 0.05 유지, 나머지: 더 큰 값 실험).  
그러나 OOD 시퀀스(large_scale)에 대해서는 근본적 개선 불가.

### 4-2. 중기 (모델 재학습, 권장)

```bash
cd src
python Network/train.py --output_dir outputs/out_classifier3
```

수정된 설정 적용:
- `window_len=200` (2초 윈도우, 현재 체크포인트 구조와 불일치 해소)
- 분류기 사전학습 이식 수정 (key prefix `"classifier."`)
- early stopping 버그 수정 완료
- scheduler 버그 수정 완료 (`vl["rmse_hard"]` → `vl["rmse"]`)

### 4-3. 장기 (시스템 개선)

- OOD 시퀀스(large_scale) 학습 데이터 추가
- IMU 노이즈 파라미터(sigma_na, sigma_ng)를 센서 캘리브레이션 값으로 교체
- 분류기 신뢰도(softmax entropy) 기반 동적 STATE_scale 조정
- `mahalanobis_fail_scale > 0` 재검토: 게이팅 실패 시 R 팽창 후 업데이트 적용

---

## 5. 파라미터 기록

| 파라미터 | 값 |
|---|---|
| window_len | 100 (현재 모델 기준) |
| update_freq | 1.0 Hz |
| sigma_na | sqrt(1e-3) ≈ 0.03162 |
| sigma_ng | sqrt(1e-4) ≈ 0.01 |
| ita_ba | 1e-4 |
| ita_bg | 1e-6 |
| init_vel_sigma | 1.0 |
| meascov_scale (CLI) | 1.0 (기본값) |
| meascov_scale (STATE) | 1차 권장값 적용 (0.3~5.0) |
| mahalanobis_fail_scale | 0 |

---

## 6. 결론

2차 테스트에서 파라미터 조정(STATE_EKF_PARAMS 상향, CLI 버그 수정)을 적용했으나 1차 대비 **전 시퀀스에서 결과 악화**.

1차 handbag 성공(2.553m)은 EKF가 제대로 동작한 것이 아니라, Mahalanobis 실패→비활성화 사이클로 인해 필터가 네트워크 dead-reckoning 모드로 우연히 동작한 것이었다.

현재 모델(`out_classifier2`) 수준에서 EKF 파라미터만으로 개선은 한계에 도달했다.  
**근본 해결책은 `window_len=200` + 수정된 학습 코드로 `out_classifier3` 재학습이다.**
