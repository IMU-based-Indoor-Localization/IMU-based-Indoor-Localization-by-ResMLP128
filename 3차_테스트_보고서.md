# EKF_basic 3차 테스트 보고서

**일시**: 2026-04-29  
**모델**: `out_classifier3/checkpoints/best.pth` (window_len=200, 수정된 사전학습 이식)  
**테스트 환경**: CUDA 학습 → CPU 추론, Oxford 24컬럼 포맷, 100Hz

---

## 1. 재학습 결과 (out_classifier3)

### 수정된 버그 (→ 재학습 근거)

| 버그 | out_classifier2 상태 | out_classifier3 수정 |
|---|---|---|
| window_len | 100 (분류기 200과 불일치) | **200** (일치) |
| extractor 이식 | 랜덤 초기화 (shape [128,60] vs [128,120]) | **완전 이식** (0개 스킵) |
| pose_classifier 이식 | 랜덤 초기화 (key "pose_classifier." 탐색 실패) | **완전 이식** (missing=0) |
| early_cnt 로직 | val 개선 시 카운트 증가 (반전) | 수정 |
| scheduler 키 | vl["rmse_hard"] KeyError | vl["rmse"] |

### 학습 성능 비교

| 지표 | out_classifier2 | out_classifier3 | 비고 |
|---|---|---|---|
| window_len | 100 (1초) | **200 (2초)** | 단위 다름 |
| Best val RMSE | 0.1769 m/1s | 0.1959 m/2s → **0.098 m/1s** | 1초 환산 시 개선 |
| Test RMSE | 0.1712 m/1s | 0.1891 m/2s → **0.095 m/1s** | 1초 환산 시 개선 |
| 분류 acc (Test) | 93.9% | **99.7%** | +5.8%p 대폭 개선 |
| 학습 시작 rmse | 1.577 m (Epoch 1) | **0.934 m (Epoch 1)** | 사전학습 이식 효과 |

사전학습 이식이 완전히 동작해 Epoch 1부터 수렴이 훨씬 빠르고 최종 성능도 개선됐다.

---

## 2. EKF 결합 테스트 결과

```bash
python View/visualize_comparison.py \
  --model_path outputs/out_classifier3/checkpoints/best.pth \
  --norm_mean  outputs/out_classifier3/norm_mean.npy \
  --norm_std   outputs/out_classifier3/norm_std.npy \
  --window_len 200 --stride 200
```

### RMSE 비교 (전체 차수)

| 시퀀스 | 1차 Net | 1차 EKF | 2차 EKF | **3차 Net** | **3차 EKF** |
|---|---|---|---|---|---|
| oxford_handheld_21 | 1.232 m | 6.350 m | 13.056 m | **1.183 m** | **4.187 m** |
| oxford_handbag_1 | 3.022 m | **2.553 m** | 28.473 m | 4.584 m | 22.287 m |
| oxford_large_scale_6 | **1.060 m** | 6.972 m | 19.196 m | 27.487 m | 26.160 m |

시각화: `result/test3_handheld_21.png`, `result/test3_handbag_1.png`, `result/test3_large_scale_6.png`

---

## 3. 원인 분석

### 3-1. handheld_21: 양방향 개선 (성공 케이스)

handheld는 훈련 분포 내 클래스. out_classifier3의 분류 acc 99.7% + 더 나은 특징 추출로:
- Network RMSE: 1.232m → **1.183m** (개선)
- EKF RMSE: 6.350m → **4.187m** (34% 개선)

EKF 개선은 모델이 더 정확한 변위 예측과 불확실도를 제공해 Mahalanobis 게이팅 통과율이 높아졌기 때문으로 판단.

### 3-2. handbag_1: Network 악화

out_classifier2(window_len=100)에서 3.022m이던 Network RMSE가 out_classifier3(window_len=200)에서 4.584m로 증가. 원인:

- **window_len=200 (2초 변위)**: 흔들림이 심한 handbag에서 2초 변위 예측은 1초보다 훨씬 어렵다. 불규칙한 흔들림이 2배 기간 동안 누적되어 예측 불확실도 증가.
- **per-window 변위 크기**: 1차(1초) 대비 2배 → 예측 오차도 절대값 기준 커짐 → dead-reckoning 누적 오차 증가

### 3-3. large_scale_6: 근본적 OOD 실패

per-window 분석 결과:

```
win0: GT=0.065m  err=0.096m  (정지에 가까운 구간)
win1: GT=1.320m  err=0.808m  (빠른 이동)
win2: GT=1.839m  err=1.862m  (대변위, 오차 ≈ GT 크기)
...
GT 평균 이동거리: 1.538 m/2s (0.769 m/s 이동 속도)
```

- large_scale_6의 실제 이동 속도(~0.77 m/s)는 훈련 분포(실내 활동, <0.3 m/s 추정)를 크게 벗어남
- 모델이 pocket(3), handheld(2)로 잘못 분류 → 소변위 예측 → 실제 대변위와 체계적 불일치
- **out_classifier2(window_len=100)에서 1.060m였던 것이 out_classifier3(window_len=200)에서 27m로 폭등한 이유**: 1초 단위 소변위(~0.77m)는 어느 정도 학습 가능한 범위였으나, 2초 대변위(~1.54m)는 훈련 데이터 분포를 완전히 벗어남

---

## 4. 학습이 올바르게 됐는가? (사용자 질문 답변)

**학습 자체는 올바르게 진행됐다.** 근거:

1. Epoch 1 val rmse=0.934m → Epoch 100 val rmse=0.1959m (정상 수렴)
2. 분류 acc=99.7% (out_classifier2 93.9% 대비 대폭 개선)
3. 훈련 분포 내 handheld_21에서 Network 및 EKF 모두 개선 확인

**large_scale_6 결과 악화는 학습 문제가 아니라 OOD 문제다:**
- 훈련 데이터: handbag/handheld/pocket/running/slow-walking/trolley (71개 실내 시퀀스)
- large_scale: 훈련 데이터에 없는 빠른 실외 이동 → 모델이 본 적 없는 패턴
- window_len=200으로 인해 OOD 영향이 더 크게 드러남

---

## 5. 향후 과제

### 단기
- `handbag` EKF 파라미터 재조정: out_classifier3의 log_var 분포가 달라졌으므로 STATE_EKF_PARAMS 재튜닝 필요
- `handheld` EKF 추가 개선: 현재 4.187m이나 Network 1.183m 수준까지 개선 여지 있음

### 중기  
- **large_scale 훈련 데이터 추가**: `cls_split_7way`에 large_scale 시퀀스를 포함시켜 재학습
  ```python
  # classification_dataset.py _FOLDER_TO_CLASS 에 추가:
  "large_scale": "large_scale",  # 새 클래스로 추가
  ```
- large_scale 클래스를 STATE_EKF_PARAMS에 등록

### 장기
- `window_len=200` EKF 업데이트 주기 최적화 (현재 1Hz, 2초 윈도우)
- 분류 신뢰도 기반 동적 meascov_scale 조정

---

## 6. 파라미터 기록

| 파라미터 | 값 |
|---|---|
| window_len | 200 |
| stride | 200 (비겹침) |
| update_freq | 1.0 Hz |
| meascov_scale (CLI) | 1.0 |
| STATE_EKF_PARAMS | 2차와 동일 (0.3~5.0) |
| mahalanobis_fail_scale | 0 |
